name: 43-deploy-main-to-taapply-prod.yml
on:
  workflow_dispatch

env:
  GITHUB_TOKEN: ${{ secrets.PAT }}

jobs:
  has_deploy_label:
    name: Launch TaApply Deploy
    runs-on: ubuntu-latest
    steps:
      - name: "Launch taapply deploy"
        run: |
            gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            /repos/ucsb-cs156-s25/dokku-sync/actions/workflows/00-sync-scaffold.yml/dispatches \
            -f ref='main' 
     
          
